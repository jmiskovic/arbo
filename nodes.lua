-- {linear, {dx, dy, rot, sx, sy, ox, oy, kx, ky}, arbo}
linear    = 'linear'
-- {intersect, arbo1, arbo2, ...}
intersect = 'intersect'
-- {lhp}
lhp       = 'lhp'
-- {join, arbo1, arbo2, ...}
join      = 'join'
-- {tint, {h, s. l}, arbo}
tint      = 'tint'
-- {wrap, arbo}
wrap      = 'wrap'
-- {simplex}
simplex   = 'simplex'
-- {negate, arbo}
negate    = 'negate'
-- {union, arbo1, arbo2, ...}
union     = 'union'
-- {set, table_index, new_value}
set       = 'set'
-- {memo, arbo}
memo      = 'memo'